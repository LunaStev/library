import("request");
import("response");
import("utils");
import("types");

fun send_get(url :str) -> HttpResponse {
    return send_get_request(url);
}

fun send_post(url :str, body :str) -> HttpResponse {
    return send_post_request(url, body);
}